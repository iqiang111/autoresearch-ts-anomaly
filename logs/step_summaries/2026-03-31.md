# Step Summaries - 2026-03-31

## Step 1

Objective: switch runtime from the broken default Python path to the Miniconda `price` environment.

Summary:
- `conda env list` was not reliable in this sandbox because conda's environment inspection hit a Windows permission error in its virtual package probing path.
- Direct execution through `conda run -n price` works.
- Verified interpreter:
  - `C:\Users\22077\miniconda3\envs\price\python.exe`
  - `Python 3.11.14`

Conclusion:
- Use `C:\Users\22077\miniconda3\Scripts\conda.exe run -n price python ...` for all Python commands in this repo.

## Step 2

Objective: confirm that the `price` environment contains the required runtime packages.

Summary:
- Verified package imports inside `price`:
  - `numpy==1.26.4`
  - `pandas==2.3.3`
  - `torch==2.5.1+cu121`
- Torch emitted a `pynvml` deprecation warning, but imports succeeded.

Conclusion:
- The environment is sufficient for basic execution and smoke tests.

## Step 3

Objective: create a dedicated folder for step-by-step progress notes.

Summary:
- Created `logs/step_summaries/`.
- This file records key actions, blockers, and decisions after each meaningful step.

Conclusion:
- Future setup, validation, and migration notes will be appended here.

## Step 4

Objective: run dataset preparation inside the `price` environment.

Summary:
- Executed `prepare.py` with `conda run -n price python prepare.py`.
- Generated default synthetic dataset at `data/synthetic_timeseries.npz`.
- Reported dataset stats:
  - `time_steps=8000`
  - `num_features=4`
  - `positive_labels=1372`
  - `positive_ratio=0.1715`

Conclusion:
- Data preparation works under the `price` environment.
- The repo now has a local synthetic dataset for smoke testing.

## Step 5

Objective: run a short end-to-end training smoke test inside `price`.

Summary:
- Launched a shortened run with:
  - `conda run -n price python -c "import train; train.TIME_BUDGET = 3; train.main()"`
- Initial run failed with:
  - `RuntimeError: cannot pin 'torch.cuda.FloatTensor' only dense CPU tensors can be pinned`
- Root cause:
  - the dataloader `collate` function moved tensors to GPU before `pin_memory` finished.

Conclusion:
- The input pipeline had a real device-transfer bug that needed code changes.

## Step 6

Objective: fix the device-transfer bug and re-run the smoke test.

Summary:
- Updated `prepare.py` so dataloader collation keeps tensors on CPU.
- Updated both training and evaluation paths to move batches to the model device explicitly.
- Re-ran syntax check with:
  - `conda run -n price python -m py_compile prepare.py train.py`
- Re-ran 3-second smoke test successfully.
- Smoke-test metrics:
  - `val_loss=0.982485`
  - `forecast_mse=0.354465`
  - `anomaly_bce=0.628020`
  - `pr_auc=0.659283`
  - `best_f1=0.655106`
  - `peak_vram_mb=354.3`

Conclusion:
- The current baseline now runs end-to-end inside the `price` environment.
- Future experiments should continue using `conda run -n price python ...`.

## Step 7

Objective: add a Chinese README and cross-links between language versions.

Summary:
- Added `README_zh.md`.
- Added a link from `README.md` to `README_zh.md`.
- Added a backlink from `README_zh.md` to `README.md`.
- Included a short note in the Chinese README about using the local `price` conda environment.

Conclusion:
- The repository now has bilingual entry documentation.
